import { WikipediaTextPipe } from './wikipedia-text-pipe';

describe('WikipediaTextPipe', () => {
  it('create an instance', () => {
    const pipe = new WikipediaTextPipe();
    expect(pipe).toBeTruthy();
  });
});
